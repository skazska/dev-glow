# yaml-language-server: $schema=../schemas/config.schema.json
# Glow Project Configuration for TaskTrack POC_1 Scenario
# This configuration represents the expected state after `glow project init`
# AI Generated

version: "0.1.0"

project_name: "TaskTrack"

# Process data storage location (relative to project root)
data_folder: "glow/"

# Process definition file (relative to .glow/)
process_config: "process_config.yaml"

# Templates folder (relative to .glow/)
templates_folder: "templates/"

# Default template for steps without classification-specific template
default_template: "any-step.md"
